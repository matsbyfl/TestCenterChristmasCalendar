package controllers;

public class AlternativeTodo {
	/*Post.find(
    "select p from Post p, Comment c where c.post = p and c.subject like ?", "%hop%"
);

SELECT c.currency, SUM(c.population)
FROM Country c
WHERE 'Europe' MEMBER OF c.continents
GROUP BY c.currency
HAVING COUNT(c) > 1

int maxWins = Integer.parseInt(Play.configuration.getProperty("tcc.winPerParticipant"));
String query = "SELECT p FROM Win w, Participant p " +
	"where p = w.winner ";
	+
	"GROUP BY w.winner";
	HAVING COUNT( w ) > ?"; 
JPAQuery jq = Participant.find( query );*/
}
